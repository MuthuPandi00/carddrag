import { CardthreeDirective } from './cardthree.directive';

describe('CardthreeDirective', () => {
  it('should create an instance', () => {
    const directive = new CardthreeDirective();
    expect(directive).toBeTruthy();
  });
});
